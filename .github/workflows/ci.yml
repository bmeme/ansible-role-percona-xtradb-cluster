name: Ansible Role Percona XraDB Cluster
'on':
  pull_request:
  push:
    branches:
      - main

defaults:
  run:
    working-directory: 'bmeme.percona_xtradb_cluster'

jobs:

  # lint:
  #   name: Lint
  #   runs-on: ubuntu-latest
  #   steps:
  #     - name: Check out the codebase.
  #       uses: actions/checkout@v2
  #       with:
  #         path: 'bmeme.percona_xtradb_cluster'

  #     - name: Set up Python 3.
  #       uses: actions/setup-python@v2
  #       with:
  #         python-version: '3.x'

  #     - name: Install test dependencies.
  #       run: pip3 install yamllint ansible-lint

  #     - name: Lint code.
  #       run: |
  #         yamllint .
  #         ansible-lint .

  molecule:
    name: Molecule
    runs-on: ubuntu-latest
    strategy:
      matrix:
        distro:
          - centos:7.9
          # - ol:8.6
    
    steps:
      - name: Check out the codebase.
        uses: actions/checkout@v2
        with:
          path: 'bmeme.percona_xtradb_cluster'

      - name: Set up Python 3.
        uses: actions/setup-python@v2
        with:
          python-version: '3.x'

      - name: Install test dependencies.
        run: pip3 install ansible molecule[docker]==4.0.3 docker

      - name: Run Molecule tests 
        run: molecule test
        env:
          PY_COLORS: '1'
          ANSIBLE_FORCE_COLOR: '1'
          MOLECULE_DISTRO: ${{ matrix.distro }}